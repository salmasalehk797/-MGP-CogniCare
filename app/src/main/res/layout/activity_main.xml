<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    tools:context=".MainActivity">

    <!-- One-time setup layout -->
    <include
        android:id="@+id/oneTimeSetupLayout"
        layout="@layout/activity_setup"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Wrap the MAIN content in a container -->
    <LinearLayout
        android:id="@+id/mainContentLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Logo -->
        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:contentDescription="@string/logo_description"
            android:src="@drawable/logo"
            android:layout_marginBottom="24dp"/>

        <!-- Daily Assessment -->
        <Button
            android:id="@+id/dailyquizbtn"
            style="@style/MainButtonStyle"
            android:text="Daily Assessment"/>

        <!-- Numbers Game -->
        <Button
            android:id="@+id/numbersgamebtn"
            style="@style/MainButtonStyle"
            android:text="Numbers Game"/>

        <!-- Word Pairing -->
        <Button
            android:id="@+id/paringgamebtn"
            style="@style/MainButtonStyle"
            android:text="Word Matching Game"/>

        <!-- Personal Info -->
        <Button
            android:id="@+id/personalbtn"
            style="@style/MainButtonStyle"
            android:text="Personal Info"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
